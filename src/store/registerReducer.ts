const registerInitState = {}

export const registerReducer = (state = registerInitState, action: any): any => {
    switch (action.type) {
        default: {
            return state
        }
    }
};